# 환경변수 파일 (절대 git에 올리면 안 됩니다!)
.env
.env.*
!.env.example

# Python 가상환경 / 캐시
.venv/
venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# SQLite DB (발급 메타 — 런타임 생성, 커밋 금지)
data/app.db
data/app.db-journal
data/app.db-wal
data/app.db-shm
*.sqlite
*.sqlite3

# 에디터 / 도구 로컬 설정
.claude/

# macOS
.DS_Store
