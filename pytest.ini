[pytest]
# async 테스트(generate_narrative 등 LLM 호출부)를 마커 없이 실행.
asyncio_mode = auto
# 테스트를 tests/ 로 옮기면서 conftest.py 의 암묵적 루트 sys.path 삽입이 사라지므로
# 선언적으로 대체: 레포 루트를 sys.path 에 추가해 `from app...` 절대 import 를 보장.
pythonpath = .
# 테스트는 tests/ 에서만 수집 (scripts/ 의 수동 스모크 스크립트 오수집 방지).
testpaths = tests
